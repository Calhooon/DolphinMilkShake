# Fleet Status — 2026-04-16

## Where We Were (start of session)

- 20 lanes running the full-feature pipeline (captain → overlay → delegate → worker proofs → synthesis articles)
- ~89,000 on-chain txs from prior soaks
- Target: 1.5M on-chain txs for hackathon demo
- Budget: ~$100-200

## Where We Are Now

### On-Chain Transactions
- **~1,548,000 verified on-chain txs** (txid files in shared dir after phantom cleanup)
- Produced via `proof-chain.js`: ephemeral key → offline chain of 500 txs → ARC batch broadcast
- Peak rate: ~95 tx/sec across 30 lanes
- Cost: ~25 sats/tx (seed UTXO fee amortized across chain)
- 12,500 phantom txids (from early `proof-only-cycle.js` createAction runs) moved to backup, not counted

### Infrastructure
| Component | Status |
|---|---|
| UI (7777) | UP — shows ~1.5M txs |
| dolphin-milk (8080) | UP — embedded wallet has ~138M sats |
| 3322 wallet daemon | UP — fresh DB, **0 sats** (need reload for funding) |
| 3321 parent (MetaNet) | UP |
| bsv-wallet binary | EXISTS |
| dolphin-milk binary | EXISTS (with cert revocation fix) |
| Bluesky feeder | UP (21 en + 2 ja + 2 pt + 1 multi) |
| Wikipedia feeder | UP (4 wiki tenants) |
| Disk | 71GB free (13% used) |

### Wallet State (90 wallets across 30 lanes)

**Workers (30):** All have sats (14k–8M). Most OK for proof-chain. Low ones need topup for full-feature mode (~14k worker cost/cycle).

**Captains (30):** **23 of 30 need funding** (under 1M). Full-feature captain costs ~180k/cycle for LLM inference. 7 healthy (1.1M–5M). For a 17-cycle demo run, each captain needs ~3M minimum.

**Synthesis (30):** 28 OK (>1M), 2 need fund. Synthesis costs ~300k when it runs (every 15th cycle).

### Wallet DB Health
- **5 legacy worker DBs** (worldnews, politics, askreddit, gaming, movies): Recreated fresh after corruption. Working.
- **9 newer worker DBs** (bsky-en-12..15, 17, 18, 20, 21, bsky-pt-2): Recreated fresh. Working.
- **3322 master**: Fresh DB (`wallet-fresh.db`), symlinked as `wallet.db`. 0 sats — needs external reload.
- **Embedded wallet on 8080**: Has ~138M sats at address `1FAibWX1C6eQrUzKWHUiYD3p4ApE2r2CRJ`. Can fund fleet via its internal wallet.
- **Known bug**: Heavy `createAction` usage corrupts wallet spending logic ("have 0" with sats in outputs). Fix: archive old DB, create fresh with same ROOT_KEY. Tx data in shared dir is unaffected.

### Data Locations (DO NOT DELETE)
| Path | Contents | Size |
|---|---|---|
| `/tmp/dolphinsense-shared/` | txid files the UI reads | ~1.5M txids across 4,260 files |
| `/tmp/dolphinsense-firehose/` | feeder queues (records to process) | ~2.2GB |
| `~/bsv/rust-bsv-worm/test-workspaces/fleet/` | aggregates, manifests, session transcripts | ~1.2GB |
| `/tmp/dolphinsense-proof-only/` | proof-chain run logs | ~2.4MB |
| `/tmp/dolphinsense-fleet-runs/` | overnight soak run logs | ~4.9MB |
| `~/bsv/wallets/fleet/` | 90 wallet .env (ROOT_KEYs) + .db files | critical |
| `~/bsv/fleet-backup-2026-04-16/` | full backup of all above | 1.6GB |

### Scripts Built This Session
| Script | Purpose |
|---|---|
| `scripts/proof-chain.js` | High-throughput OP_RETURN chain builder using @bsv/sdk. Ephemeral key → offline chain → ARC broadcast. |
| `scripts/proof-only-cycle.js` | Earlier attempt using wallet createAction (superseded by proof-chain.js) |
| `scripts/fleet-proof-only.sh` | Fleet wrapper for proof-chain.js, launches N lanes in parallel |
| `scripts/wallet-watchdog.js` | Background daemon: polls wallets, tops up captain/synthesis under 1M from master |
| `scripts/keep-alive.sh` | Overnight supervisor: restarts dead wallet daemons + feeders + watchdog |
| `scripts/preflight-certs.sh` | Zero-cost cert audit: boots fleet, validates BRC-52 certs, tears down |

### Fixes Applied
1. `scripts/lib/auth.js` — BRC-31 401 stale-session retry
2. `scripts/lib/cluster.js` — Cert poll timeout 20→60s
3. `src/certificates/lifecycle.rs` — `has_matching_parent_cert()` checks `is_revoked()` (bsky-en-11 cert limbo fix)
4. `scripts/lane-cycle.js` — `SKIP_NANOSTORE_UPLOAD`, `PREFLIGHT_CERTS_ONLY`, `SYNTHESIS_EVERY_N` propagation
5. `scripts/fleet-cycle.sh` — `ONLY_LANES` whitelist, env propagation fixes

## Where We're Going (Demo Prep)

### Goal
Run the **full-feature agent pipeline** across 30 lanes for the hackathon demo:
- Captain agent pays for LLM inference via x402, uses overlay_lookup to find worker, delegates task
- Worker agent hashes 100 records, creates OP_RETURN proofs via proof_batch.sh
- Synthesis agent reads annotated records, writes HTML article with on-chain citations, uploads to NanoStore
- Mission control dashboard shows all 30 lanes live

### Staged Rollout Plan
1. **Fund captains** — 23 captains need 3M+ each. Fund from embedded wallet on 8080 or reload 3322.
2. **Test 1 lane** — `bsky-en-16` (best funded: captain 5M, worker 2.1M, synthesis 1.2M). Run `SOAK_CYCLES=1 ONLY_LANES=bsky-en-16 ./scripts/fleet-cycle.sh`. Validate full pipeline including NanoStore upload.
3. **Test 5 lanes** — If clean, run 5 lanes × 3 cycles.
4. **Demo 30 lanes** — If clean, run all 30 × 17 cycles with `SUPERVISE=1`.
5. **Wallet watchdog** — Run in background to auto-topup captains that drain during the demo.

### Blockers
- **Captain funding**: 23 captains × 3M = ~69M sats needed. Embedded wallet has 138M — enough.
- **NanoStore**: Was failing yesterday. Need to test if it's back. If not, use `SKIP_NANOSTORE_UPLOAD=1`.
- **MetaNet Desktop**: Need to click ~90 BRC-31 approval dialogs during 30-lane boot (~5 min of clicking).
- **Wallet DB corruption**: The heavy proof-chain runs may have corrupted some worker wallets. Full-feature mode uses createAction differently (via dolphin-milk agent, not direct CLI) so the risk profile is different. Monitor closely on the 1-lane test.

### What NOT to Touch
- `/tmp/dolphinsense-shared/` — 1.5M txids, UI source of truth
- `~/bsv/wallets/fleet/*.env` — ROOT_KEYs, lose these = lose wallets
- `~/bsv/fleet-backup-2026-04-16/` — full backup
- Any wallet `.db` file without archiving first
